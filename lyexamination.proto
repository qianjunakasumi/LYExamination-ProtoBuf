syntax = "proto3";

option go_package = "github.com/lyexamination/api/main";

service LYExamination {
    rpc Login(LoginReq) returns (LoginRsp) {}
}

message LoginReq {
    string phone = 1;
    string pwd = 2;
}

message LoginRsp {
    string session = 1;
}
